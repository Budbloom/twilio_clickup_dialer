# URL to the backend token endpoint
# Local development with Express: http://localhost:3001/token
# Netlify deployment (uses redirects): /api/token
VITE_TOKEN_ENDPOINT=http://localhost:3001/token

# Optional default agent identity to request from the token endpoint
VITE_TWILIO_DEFAULT_IDENTITY=clickup-agent
